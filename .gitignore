# Build artifacts
/target

# Cargo.lock is checked in for binaries, but if this were a library-only
# workspace you'd ignore it. Keeping it tracked since genomelens-cli is a bin.

# Editor / IDE
*.swp
*.swo
*~
.idea/
.vscode/
*.iml

# OS
.DS_Store
Thumbs.db

# Rust
**/*.rs.bk

# Environment / secrets
.env
.env.*

# Large test data (exceeds GitHub 50MB limit)
testdata/*.vcf.gz
!testdata/small.vcf.gz

# Agents
.claude/
